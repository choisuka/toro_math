# toro_math 프로젝트 지침

## 0. 세션 시작 체크리스트

사용자의 첫 메시지에 응답하기 전, 반드시 체크리스트 완료를 먼저 선언한다.

- [ ] CLAUDE.md 읽었음
- [ ] MEMORY.md 및 연결된 memory 파일들 읽었음
- [ ] 답변 규칙 인지함: 모르면 '모르겠습니다', 추측이면 '추측입니다', 출처 명시, 모호하면 먼저 질문
- [ ] 핵심 피드백 규칙 인지함: 기존 카드 수정 금지·새 카드로 추가 / 콘텐츠 변경 전 원문·변경 형식 확인 후 사전 승인 / 옮길 때 원문 그대로(요약·재구성 금지) / 분수는 frac() 함수(슬래시 금지) / 커밋 전 git status 확인·add -A 금지 / 커밋과 푸시 분리(푸시는 명시 요청 시만) / HTML 수정 후 자동으로 브라우저 열기
- [ ] `_dev/examprep_reference_*.txt` 존재 확인함 — 있으면 문제/카드 생성 전 필독 (아래 4번 참고)

## 1. 기능 구현 후 테스트

기능 구현이 끝나면 반드시 테스트까지 완료한 뒤 보고한다.
- 브라우저에서 해당 기능을 직접 실행하여 동작 확인
- 엣지 케이스(경계값, 빈 입력 등) 포함

## 2. 작은 단위로 커밋

변경은 작은 단위로 나눠서 커밋한다.
- 하나의 커밋 = 하나의 논리적 변경 (카드 추가, 버그 수정, 스타일 조정 등)
- 여러 기능을 한 번에 커밋하지 않는다

## 3. 기술 스택

| 항목 | 내용 |
|------|------|
| 구조 | 단일 파일 앱 (`index.html`) — HTML + CSS + JS 통합 |
| 프레임워크 | 없음 (Vanilla JS) |
| 빌드 도구 | 없음 |
| 폰트 | Google Fonts — Jua, Nanum Gothic |
| AI 연동 | `toro_ai.js` (외부 스크립트) |
| 언어 | 한국어 UI |

## 4. 시험대비/문제 생성 시 참고자료 (필수)

`_dev/examprep_reference_*.txt` 파일이 있으면, 문제나 카드를 새로 만들기 전에 **반드시 먼저 Read해서 훑어본다**. 기억이나 짐작으로 "이 정도면 어렵겠지"를 재구성하지 않는다.

- 현재 파일: `examprep_reference_도형의방정식_2026.txt`, `examprep_reference_집합과명제_함수_2026.txt`, `examprep_reference_다항식_2026.txt` (`examprep_reference_gangnam_2026.txt`는 보조자료)
- 출처: `D:\내신기출`의 실제 학교 기출 hwp 파일을 `pyhwp`(`hwp5txt` CLI)로 텍스트 추출. 강남권 내신 기준, 숫자·수식 일부는 추출 과정에서 유실될 수 있지만 문제 구조·조건 조합 패턴은 실제 그대로다.
- `.claude/agents/`의 세 에이전트(`toro-variant-generator`, `toro-advanced-generator`, `toro-examprep-generator`)는 이미 이 참고자료를 절차에 반영해 자동으로 읽는다(글롭 패턴이라 새 파일이 추가돼도 별도 수정 없이 인식됨).
- 새 단원의 참고자료 파일이 필요한데 없다면, 사용자에게 D:\내신기출에서 관련 hwp 파일을 더 추출할지 먼저 확인한다(hwp5txt는 2013~2017년 학교명 표기 파일에서 잘 동작하고, 2018년 이후 코드명 파일은 추출 실패가 잦으니 배치 작업 전 개별 테스트 필요).
- 자세한 배경(추출 방법, 신뢰도 패턴, 탐색 안 한 영역)은 memory `project_toro_math.md` 참고.

## 5. 메모리 파일 관리 기준

사용자 원문(2026-09-09, 메모리 파일 대정리 작업 중): "훌륭한 에이전트를 만드는 과정이니 핵심사안, 반복사안, 치명적 사안, 사용자에 큰 영향을 주는 내용들에 집중하고 내가 쉽게 알아차릴 수 있는 내용은 굳이 해서 너 집중도를 흩어지게 할 것 같아."

- 남길 것: 핵심 사안 / 반복적으로 재발한 사안 / 치명적(기능 오류·사고로 이어짐) 사안 / 사용자에게 큰 영향을 주는 사안
- 안 남길 것(또는 삭제할 것): 사용자가 옆에서 보면 쉽게 알아차리고 그 자리에서 바로 고쳐질 정도로 사소한 것, 심각도가 낮고 복구 비용이 낮은 것, 최초 지적 이후 오랫동안 재발 기록이 없는 것
- 판단 절차: (1) 이 메모리를 지우면 구체적으로 어떤 실수를 다시 하게 되는지 사례로 확인 (2) 그 실수의 심각도(치명적 vs 소통 마찰 수준)를 판단 (3) 마지막 지적 이후 재발 기록이 있는지 확인 — 세 가지가 다 낮으면 삭제, 하나라도 높으면 유지.
